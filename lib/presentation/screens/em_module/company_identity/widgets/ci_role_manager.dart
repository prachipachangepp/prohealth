import 'package:flutter/cupertino.dart';

class CiRoleManager extends StatelessWidget {
  const CiRoleManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
