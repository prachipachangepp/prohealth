import 'package:flutter/material.dart';

class ForgetPasswordTablet extends StatelessWidget {
  const ForgetPasswordTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Forget Pass Tab"),
      ),
    );
  }
}
