import 'package:flutter/material.dart';

class CompensationChildTabbar extends StatelessWidget {
  const CompensationChildTabbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(child: Text('Compensation'),),
    );
  }
}
