import 'package:flutter/material.dart';

class ScreenFrameTab extends StatelessWidget {
  const ScreenFrameTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
