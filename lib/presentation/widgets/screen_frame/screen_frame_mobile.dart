import 'package:flutter/material.dart';

class ScreenFrameMobile extends StatelessWidget {
  const ScreenFrameMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
