import 'package:flutter/material.dart';

class ForgetPasswordWeb extends StatelessWidget {
  const ForgetPasswordWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Forget Pass web"),
      ),
    );
  }
}
