import 'package:flutter/material.dart';
/// to do prachi
class OnboardingQualification extends StatelessWidget {
  const OnboardingQualification({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white54,
    );
  }
}
